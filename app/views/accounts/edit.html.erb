<div class="profile-form">
  <h1>Editing Account</h1>

  <%= render 'form', account: @account %>
  <div class="acc-edit">
    <%= link_to 'Show', @account, class:"btn btn-success" %> |
    <%= link_to 'Back', account_path(@account), class:"btn btn-dark" %>
  </div>
</div>